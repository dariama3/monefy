services:
  frontend:
    build:
      context: ./frontend
    container_name: nuxt
    ports:
      - "3000:3000"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    environment:
      NUXT_PUBLIC_API_URL: http://localhost:8000/api
